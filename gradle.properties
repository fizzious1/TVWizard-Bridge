org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
kotlin.code.style=official

# Bridge runtime config — override with -PrelayUrl=... -PbridgeToken=... -PdeviceId=...
# The wss:// URL is derived from relayUrl by swapping the scheme.
relayUrl=https://tv.djwizard.ai
bridgeToken=
deviceId=emu-1
